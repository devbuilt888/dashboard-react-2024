import react from "react";
import { StyledChartWrapper } from "./ChartWrapper.styled";

const ChartWrapper = () => {
  return (
    <StyledChartWrapper className="chartWrapper">
      ChartWrapper
    </StyledChartWrapper>
  );
};

export default ChartWrapper;
